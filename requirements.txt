huggingface_hub>=0.24
pandas>=2.0
numpy>=1.24
soundfile>=0.12
pyyaml>=6.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.7
pyarrow>=14.0
tqdm>=4.66

# Detection backends (install ONE, see README):
#   pip install aria-inference           # recommended, ARIA ensemble
#   pip install aria-inference-birdnet   # BirdNET-based fallback
